name: ${project.name}
main: ${bukkit.main.class}
author: Cyprias
authors: [sarhatabaot]
api-version: "1.13"
prefix: "CSL"
description: ${project.description}
website: ${project.url}
version: ${project.version}
load: POSTWORLD
commands:
  cslreload:
    description: "Reloads the config file."
    permission: "csl.reload"
    permission-message: You do not have §c<permission>